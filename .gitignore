# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Env files (never commit secrets or NODE_ENV — it breaks Vercel SSR builds)
.env
.env.*
!.env.example

# Generated secret material. .secrets-generated.json was committed in 18cd4b2
# and reached origin/main with real JWT_SECRET / IDENTITY_SECRET / AUDIT_HMAC_KEY
# / SETUP_KEY values; untracked here so it cannot happen again.
.secrets-generated.json
*.secrets.json
secrets-*.json

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Vercel
.vercel/


# Generated Claude Design canvas payload (~2.3MB of editor code).
# Regenerate from the .dc.html sources with the design seed helper.
design/embrace-patient-portal.html
